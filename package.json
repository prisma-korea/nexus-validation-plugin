{
  "name": "nexus-validation-plugin",
  "private": true,
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "lint": "eslint --ext .ts,.tsx,.js,.jsx src",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
